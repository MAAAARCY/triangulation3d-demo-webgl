export default function CameraSensitivity() {
  return (
    <section className="camera-sensitivity">
      <h2 className="sidebar-content-title">
        Camera Sensitivity
      </h2>
      <div className="camera-sensitivity__slider-container">
        {/* スライダーUI */}
      </div>
    </section>
  );
}